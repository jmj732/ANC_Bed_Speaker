# ANC Daily Log — 2026-04-23

## 오늘 변경사항

### 1. anc.c 단일파일 → 멀티파일 모듈 분리 (refactor)
- `anc_main.c`: 진입점, RT 스레드 설정
- `anc_app.c/h`: 앱 초기화 및 파라미터 관리
- `anc_run.c/h`: FxLMS 메인 루프
- `anc_nb.c/h`: 협대역 pitch-tracking ANC
- `anc_pass.c/h`: passthrough 모드
- `anc_sim.c/h`: 시뮬레이션 유틸리티
- `snore_anc.c`: 코골이 전용 ANC 엔트리
- Makefile: 모듈 빌드 시스템으로 업데이트

### 2. VSS 적응 스텝 및 NEON 지원 추가 (feat)
- VSS_ALPHA/VSS_GAMMA 기반 Variable Step Size 적응
- aarch64/ARM NEON 조건부 컴파일 지원
- MU_CEIL 0.008f로 조정 (VSS 안정성)
- OUTPUT_SAFETY_LPF_HZ 200Hz 추가
- O3 + cortex-a76 빌드 플래그 반영

### 3. systemd 서비스 및 IRQ affinity 스크립트 업데이트 (chore)
- `scripts/anc-irq-affinity.sh` 수정
- `systemd/anc-rt.service` 수정
- `systemd/anc-snore.service` 추가

### 4. 문서 전면 개편 (docs)
- README 배지·다이어그램·표·알고리즘 개요 추가
- `docs/ANC_RT_SETUP.md`, `docs/README.md` 수정
- `docs/daily/2026-04-09.md`, `docs/daily/2026-04-15.md` 추가
- `docs/handoff/NEXT_SESSION_PROMPT.md` 수정

## 테스트 결과
- 이 세션은 구조 개편 및 기능 추가 위주 — 실기 측정 수치 없음
- VSS 파라미터(VSS_ALPHA, VSS_GAMMA) 실기 검증 미완
- 모듈 분리 후 빌드/xrun 안정성 검증 필요

## 분석
- **가장 큰 구조 변화**: 단일 파일 → 6개 모듈 분리로 유지보수성 및 빌드 유연성 향상
- **VSS 적응 스텝**: 고정 mu 대신 신호 에너지 기반 동적 조절 → 수렴 속도와 안정성 균형 개선 기대
- **NEON SIMD 지원**: Pi 5 (Cortex-A76) 벡터 연산 활용 → 처리 성능 향상 가능
- broadband ref_lead=0 한계는 여전히 유효 — nb 모드가 주 전략

## 다음 할일
1. 모듈 분리 후 실기 빌드 및 xrun 없는 안정 동작 검증
2. VSS 파라미터(VSS_ALPHA, VSS_GAMMA) 튜닝 실험
3. nb 모드 실제 코골이 환경 장기 테스트 (현재 최대 +4.2dB 감쇠 설정 기준)

## 참고
- 현재 상태: narrowband(nb) 모드가 주 전략, broadband는 ref_lead=0 한계로 보류
- 핵심 원칙: 한 번에 하나씩 변경, ALSA + 표준 C만 사용
