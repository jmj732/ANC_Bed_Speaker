[Unit]
Description=Set CPU governor to performance for ANC
After=local-fs.target

[Service]
Type=oneshot
ExecStart=/bin/bash -lc 'for f in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do echo performance > "$f"; done'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
